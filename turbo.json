{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalDependencies": ["pnpm-lock.yaml", "tsconfig.json", ".prettierrc.cjs", "tooling/**"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "build-storybook": {
      "dependsOn": ["^build"],
      "inputs": ["**/*.stories.ts", "**/*.stories.tsx", ".storybook/**", "packages/ui/src/**"],
      "outputs": ["storybook-static/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", "eslint.config.mjs"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "inputs": ["**/*.ts", "**/*.tsx", "tsconfig.json"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["**/*.ts", "**/*.tsx", "vitest.config.ts"],
      "outputs": ["coverage/**"]
    },
    "vrt:storycap": {
      "dependsOn": ["build-storybook"],
      "outputs": [".vrt/storycap/**"]
    },
    "vrt:lostpixel": {
      "dependsOn": ["build-storybook"],
      "outputs": [".vrt/lostpixel/**"]
    },
    "clean": {
      "cache": false
    }
  }
}
